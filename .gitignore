# Android / Gradle
.gradle/
build/
local.properties
*.apk
*.aab
*.keystore

# Go
server/linux-terminal-server

# IDE
.idea/
*.iml
.vscode/

# Secrets — nothing should reach here anyway
.env
*.pem
*.key

# The agent configuration is shared; only the per-machine half is not. Skills,
# hooks and settings.json are the repository's rules and belong to everyone
# working on it.
.claude/settings.local.json

*.log
